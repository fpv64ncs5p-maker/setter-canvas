# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# The keep-alive workflow is added through GitHub's web editor instead:
# pushing workflow files needs a token with `workflow` scope, which the
# current one lacks. Remove this line once the token is sorted.
.github/workflows/keepalive.yml
